class Defaults:
    #Class that has global static variables for default values (like window size, plp..etc)
    PLP = 0.5
    P_CORRUPTION = 0
    WINDOW_SIZE = 5
    SELECTIVE_REPEAT = True
    MAX_SEQN = 1024
    TIMEOUT = 0.2
    STOP_WAIT = True #overrides Selective repeat flag
